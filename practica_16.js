// Declarar el array llamado nombres con los valores `Juan` y `Pedro`
let nombres = [`Juan`, `Pedro`, `Mimi`];
// Recorrer el array nombres con un for y imprimir en pantalla sus valores
for(var pos=0; pos<nombres.length; pos++) {
 let valor = nombres[pos];
 // los imprime uno arriba de otro xq es un for entonces imprinme de a 1
 console.log(valor)
}
console.log(nombres);