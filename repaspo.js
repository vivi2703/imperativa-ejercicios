/*function puedeViajar(edad, numero1, entrada)
{
    if (edad <18 || entrada==false) 
    {
        return "Deberias comprar entradas o";
    }
    else if()
    {
        return false;
    }
}
function sumar(numero1, numero2)
{
    let suma=numero1+numero2;
    let multiplicacion=numero1*numero2;
    if (suma%2===o){
        return "Se a pasado la condicion";
    }
    else if(multiplicacion%2!==0)
    {
        return false;
    }
    return -1
}


function sumar(numero1, numero2)
{
    let suma=numero1+numero2;
    let multiplicacion=numero1*numero2;
    if (suma%2===o){
        return "Se a pasado la condicion";
    }
    else if(multiplicacion%2!==0)
    {
        return false;
    }
    return -1
}
console.log(sumar(3,5));
console.log(puntajeViajar(18,false));*/

const edades = [11, 12, 15, 18, 25, 22, 10, 33, 18, 5];

function edadesMenores(edades)

  let arraymenores=[];
  for(let i=0; i<edades.length;i=++);

  {

  }